import styles from "../styles/components/logo.module.scss"

const Logo = (props) => {
      return (
            <img src="./images/logo.svg" alt="Insure Logo" className={styles.logo}/>
      )
}

export default Logo